import { PlaceholderPage } from '@/components/PlaceholderPage';

export function MyMatches() {
  return (
    <PlaceholderPage
      title="My Match Requests"
      description="View all instances where the algorithm matched your inventory to a buyer's search. See match score, buyer requirements, and conversion rate."
      module="Matching Engine"
    />
  );
}
