import { PlaceholderPage } from '@/components/PlaceholderPage';

export function SupplierNotifications() {
  return (
    <PlaceholderPage
      title="My Notifications"
      description="All notifications: new orders, status updates, low stock alerts, ratings received, match requests, payments. Real-time via WebSocket."
      module="Notifications"
    />
  );
}
