import { PlaceholderPage } from '@/components/PlaceholderPage';

export function BulkUpload() {
  return (
    <PlaceholderPage
      title="Bulk Upload"
      description="Upload inventory via CSV/Excel for mass additions or updates. Map columns, preview changes, and batch import."
      module="Supplier & Inventory"
    />
  );
}
