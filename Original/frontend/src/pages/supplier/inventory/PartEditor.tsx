import { PlaceholderPage } from '@/components/PlaceholderPage';

export function PartEditor() {
  return (
    <PlaceholderPage
      title="Add / Edit Part"
      description="Form to add a new part or edit existing — part name, description, category, compatible vehicles, price, quantity, images, and SKU."
      module="Supplier & Inventory"
    />
  );
}
