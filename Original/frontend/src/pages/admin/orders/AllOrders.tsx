import { PlaceholderPage } from '@/components/PlaceholderPage';

export function AllOrders() {
  return (
    <PlaceholderPage
      title="All Orders"
      description="View every order on the platform with status filters (pending, confirmed, in-transit, delivered, cancelled, disputed). Full search, sort, and export."
      module="Order Management"
    />
  );
}
