import { PlaceholderPage } from '@/components/PlaceholderPage';

export function DisputedOrders() {
  return (
    <PlaceholderPage
      title="Disputed Orders"
      description="Orders flagged with issues that need admin intervention. Resolve disputes, cancel orders, and view full order timelines."
      module="Order Management"
    />
  );
}
