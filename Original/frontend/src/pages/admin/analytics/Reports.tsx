import { PlaceholderPage } from '@/components/PlaceholderPage';

export function AdminReports() {
  return (
    <PlaceholderPage
      title="Reports"
      description="Generate detailed reports: sales, supplier performance, buyer activity, inventory turnover, route efficiency. Export as PDF/CSV with date range filters."
      module="Analytics"
    />
  );
}
