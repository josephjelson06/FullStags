import { PlaceholderPage } from '@/components/PlaceholderPage';

export function RolesPermissions() {
  return (
    <PlaceholderPage
      title="Roles & Permissions"
      description="Define roles (admin, supplier, buyer), assign granular permissions per module, and control who can access what across the platform."
      module="Auth / RBAC"
    />
  );
}
