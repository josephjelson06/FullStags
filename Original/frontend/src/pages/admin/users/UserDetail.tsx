import { PlaceholderPage } from '@/components/PlaceholderPage';

export function UserDetail() {
  return (
    <PlaceholderPage
      title="User Detail"
      description="View a single user's profile, assign or change roles, activate/deactivate accounts, reset passwords, and view login activity logs."
      module="Auth / RBAC"
    />
  );
}
