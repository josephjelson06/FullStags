import { PlaceholderPage } from '@/components/PlaceholderPage';

export function UserManagement() {
  return (
    <PlaceholderPage
      title="User Management"
      description="View all registered users (suppliers & buyers) in a searchable, filterable table. Create, edit, activate, or deactivate accounts."
      module="Auth / RBAC"
    />
  );
}
