import { PlaceholderPage } from '@/components/PlaceholderPage';

export function MatchConfig() {
  return (
    <PlaceholderPage
      title="Matching Configuration"
      description="Set algorithm weights — control how much importance to give price vs distance vs supplier rating vs stock availability in the matching engine."
      module="Matching Engine"
    />
  );
}
