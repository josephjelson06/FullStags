import { PlaceholderPage } from '@/components/PlaceholderPage';

export function MatchLogs() {
  return (
    <PlaceholderPage
      title="Match Logs"
      description="View history of all matches made — which buyer was matched to which supplier, match score breakdown, failed or low-confidence matches."
      module="Matching Engine"
    />
  );
}
