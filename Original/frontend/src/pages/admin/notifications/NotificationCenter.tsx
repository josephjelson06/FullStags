import { PlaceholderPage } from '@/components/PlaceholderPage';

export function NotificationCenter() {
  return (
    <PlaceholderPage
      title="Notification Center"
      description="View all platform-level notifications and alerts. Send announcements to all users or specific roles, monitor WebSocket health."
      module="Notifications"
    />
  );
}
