import { PlaceholderPage } from '@/components/PlaceholderPage';

export function BuyerNotifications() {
  return (
    <PlaceholderPage
      title="My Notifications"
      description="All notifications: order confirmed, status updates, price drops on frequently ordered parts, and match results. Real-time via WebSocket."
      module="Notifications"
    />
  );
}
