import { PlaceholderPage } from '@/components/PlaceholderPage';

export function MatchDetail() {
  return (
    <PlaceholderPage
      title="Match Detail"
      description="Full breakdown of why a supplier was recommended — score per factor, supplier profile, part details, and estimated delivery time."
      module="Matching Engine"
    />
  );
}
